apiVersion: apps/v1
kind: Deployment
metadata:
  name: mysql-orders
  namespace: shopmicro
spec:
  replicas: 1
  selector:
    matchLabels:
      app: mysql-orders
  template:
    metadata:
      labels:
        app: mysql-orders
    spec:
      containers:
      - name: mysql
        image: mysql:8
        ports:
        - containerPort: 3306
        envFrom:
        - configMapRef:
            name: mysql-orders-config
        - secretRef:
            name: mysql-orders-secret
